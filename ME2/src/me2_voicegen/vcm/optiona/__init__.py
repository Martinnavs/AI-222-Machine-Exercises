"""Option A: the toy/original VCM dataset's own grammars and canonical
intent-phrase table (`SPEC_GRAMMAR`/`TOY_GRAMMAR` in `grammar.py`,
`INTENT_PHRASES` in `phrases.py`). Mirrors the sibling `vcm.optionb` package's
convention: only this experiment's own content lives here, all the generic
CTC machinery lives in the parent `vcm` package.
"""
