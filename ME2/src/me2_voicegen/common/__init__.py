"""Cross-methodology-agnostic machinery, usable by the current character-CTC
voice-command approach or a future phoneme/WFST-based approach without
modification: `grammar_core` (grammar combinators/trie), `augment`
(waveform-level augmentation), `features` (log-mel front end).
"""
